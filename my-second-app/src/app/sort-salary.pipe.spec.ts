import { SortSalaryPipe } from './sort-salary.pipe';

describe('SortSalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
